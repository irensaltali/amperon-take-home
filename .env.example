# Tomorrow.io API Configuration (REQUIRED)
# Get your API key from: https://www.tomorrow.io/weather-api/
TOMORROW_API_KEY=your_api_key_here

# Tomorrow.io API Settings (Optional - defaults shown)
# TOMORROW_API_BASE_URL=https://api.tomorrow.io/v4
# TOMORROW_API_TIMEOUT_SECONDS=30
# TOMORROW_API_MAX_RETRIES=5
# TOMORROW_API_RETRY_DELAY_SECONDS=1.0

# PostgreSQL Database Configuration (Optional - defaults shown)
PGHOST=localhost
PGPORT=5432
PGDATABASE=tomorrow
PGUSER=postgres
PGPASSWORD=postgres
# PG_POOL_SIZE=5
# PG_POOL_TIMEOUT_SECONDS=30

# Pipeline Configuration (Optional - defaults shown)
# FETCH_INTERVAL_MINUTES=60
# FETCH_FORECAST_HOURS=120
# FETCH_HISTORICAL_HOURS=24
# DATA_GRANULARITY=hourly

# Logging Configuration (Optional - defaults shown)
# LOG_LEVEL=INFO
# LOG_FORMAT=json

# Environment (Optional - defaults shown)
# ENVIRONMENT=development
# DEBUG=false
