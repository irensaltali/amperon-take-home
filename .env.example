# Tomorrow.io API Configuration (REQUIRED)
# Get your API key from: https://www.tomorrow.io/
TOMORROW_API_KEY=your_api_key_here

# Database Configuration
# For Docker: These connect to the postgres service container
# For local dev: Change PGHOST to localhost
PGHOST=postgres      # Use 'localhost' for local dev, 'postgres' for Docker
PGPORT=5432
PGDATABASE=tomorrow
PGUSER=postgres
PGPASSWORD=postgres

# Application Settings
LOG_LEVEL=INFO
