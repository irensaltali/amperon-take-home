[DEFAULT]
sources = %(here)s
migration_table = _yoyo_migration
batch_mode = on
verbosity = 1

[postgresql]
driver = psycopg2
host = %(env:PGHOST:localhost)s
port = %(env:PGPORT:5432)s
database = %(env:PGDATABASE:tomorrow)s
user = %(env:PGUSER:postgres)s
password = %(env:PGPASSWORD:postgres)s
